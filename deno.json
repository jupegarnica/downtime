{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "test": "deno test --A",
    "cli": "deno run -A cli.ts",
    "install": "deno install -fn downtime cli.ts",
    "server": "deno run -A server.ts"
  }
}
